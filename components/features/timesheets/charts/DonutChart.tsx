// The Timesheets donut is now the shared, app-wide animated donut.
// Kept as a re-export so existing imports (DonutPanel) don't need to change.
export { default, type DonutSlice } from '@/components/ui/AnimatedDonut';
